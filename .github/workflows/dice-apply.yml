name: Dice Easy Apply (DISABLED)

# DISABLED: Dice blocks headless Chrome. Click-counting, no real applies.
# Re-enable after building proper Dice API integration.
on:
  workflow_dispatch: {}
  # schedule DISABLED:
  # - cron: "30 13 * * 1-5"

jobs:
  dice-apply:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"

